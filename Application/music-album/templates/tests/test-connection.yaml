apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "music-album.fullname" . }}-test-connection"
  labels:
    {{- include "music-album.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "music-album.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
